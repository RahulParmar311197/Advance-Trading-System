type Result={status?:string;data_version?:string;candle_count?:number;signal_count?:number;trade_count?:number;metrics?:Record<string,unknown>;trades?:Array<Record<string,unknown>>};
const show=(v:unknown)=>v===null||v===undefined?"—":typeof v==="number"?v.toFixed(4):String(v);
export default function BacktestResults({result}:{result:Result|null}){if(!result)return <div className="muted">Run a backtest to see results.</div>;const m=result.metrics||{};return <><div className="metrics">{["total_return","win_rate","profit_factor","expectancy","max_drawdown","average_trade"].map(k=><div className="metric" key={k}><span className="muted">{k.replaceAll("_"," ")}</span><b>{show(m[k])}</b></div>)}</div><p className="muted">{result.trade_count??0} trades · {result.signal_count??0} signals · data {result.data_version}</p>{(result.trades||[]).length>0&&<table className="table"><thead><tr><th>Direction</th><th>Entry</th><th>Exit</th><th>Net P&L</th></tr></thead><tbody>{result.trades!.map((t,i)=><tr key={i}><td>{String(t.direction)}</td><td>{show(t.entry)}</td><td>{show(t.exit)}</td><td>{show(t.net_pnl)}</td></tr>)}</tbody></table>}</>}
